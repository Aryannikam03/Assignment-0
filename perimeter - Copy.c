#include <stdio.h>
int main()
{
	int length;
	length=10;
	int width;
	width=30;
	int perimeter;
	perimeter=2*(length+width);
	printf("length of %d :%d\n",width,perimeter);
	printf("width of %d :%d\n",length,perimeter);
	return 0;
}